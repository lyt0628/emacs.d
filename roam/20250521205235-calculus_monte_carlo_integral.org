:PROPERTIES:
:ID:       058bf416-b5a8-4eb6-adb3-c1417f68c1e4
:END:
#+title: calculus/monte-carlo-integral

蒙特卡罗积分是通过‌随机采样‌估算积分值的数值方法
其核心思想可概括为：
将积分转换为‌概率期望‌的计算，通过大量采样点估算积分值
利用‌大数定律‌保证采样数量足够大时，估计值收敛于真实值


对积分 I=∫abf(x)dxI=∫ab​f(x)dx，蒙特卡罗积分通过以下步骤估算：

‌概率转换‌：引入概率密度函数（PDF）p(x)p(x)，满足 ∫abp(x)dx=1∫ab​p(x)dx=1，则积分可表示为期望形式：
I=E[f(x)p(x)]I=E[p(x)f(x)​]6。
‌采样估计‌：生成NN个服从p(x)p(x)分布的样本{xi}{xi​}，计算估计值：
I^=1N∑i=1Nf(xi)p(xi)I^=N1​∑i=1N​p(xi​)f(xi​)​36。

2. 均匀分布特例

当p(x)=1b−ap(x)=b−a1​（均匀分布）时，公式简化为：
I^=(b−a)⋅1N∑i=1Nf(xi)I^=(b−a)⋅N1​∑i=1N​f(xi​

为什么可以转化为概率. 要知道，在数学上，概率只是一个函数而已.
所有的函数，只要能找到一个所谓的概率密度函数，使得其定积分为1,
就能用概率的方法度量它.

